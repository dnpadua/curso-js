const marcas = ['Fiat', 'Renault'];

const modelos = ['Argo', 'Cronos', 'Captur'];

const listaDeCarros = [marcas, modelos]

console.log(`Temos apenas um ${listaDeCarros[0][0]} ${listaDeCarros[1][1]} disponível em nosso estoque para venda imediata!`);